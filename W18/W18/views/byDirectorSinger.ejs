<%- include('partials/header') %>

<!-- (f) List specified Music Director songs sung by specified Singer -->
<div class="page-header">
  <h1>Songs by Director &amp; Singer</h1>
  <p>Filter songs by music director and singer combination</p>
</div>

<div class="card">
  <form action="/by-director-singer" method="GET" class="search-form">
    <div class="form-group">
      <label for="director">Music Director</label>
      <input type="text" id="director" name="director" placeholder="e.g. Pritam" value="<%= director %>" required />
    </div>
    <div class="form-group">
      <label for="singer">Singer</label>
      <input type="text" id="singer" name="singer" placeholder="e.g. Arijit Singh" value="<%= singer %>" required />
    </div>
    <button type="submit" class="btn btn-primary">🔍 Search</button>
  </form>
</div>

<% if (director && singer) { %>
  <span class="badge">Results: <%= songs.length %> song(s)</span>
  <div class="card">
    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>#</th>
            <th>Song Name</th>
            <th>Film</th>
            <th>Music Director</th>
            <th>Singer</th>
          </tr>
        </thead>
        <tbody>
          <% if (songs.length === 0) { %>
            <tr><td colspan="5" class="no-results">No songs found for this combination.</td></tr>
          <% } else { %>
            <% songs.forEach((song, i) => { %>
              <tr>
                <td><%= i + 1 %></td>
                <td><%= song.Songname %></td>
                <td><%= song.Film %></td>
                <td><%= song.Music_director %></td>
                <td><%= song.Singer %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
<% } %>

<%- include('partials/footer') %>
